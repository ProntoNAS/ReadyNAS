
TODO:

 *	SunOS portability? Add *.adjunct maps
 *	Synchronize with several apps that lock map sources.

DONE:

 *	Autoconfig when building: find the map sources & various dirs:
	/usr/lib/aliases vs. /etc/aliases
	/usr/lib/yp	 vs. /usr/etc/yp
	and so on.


/* Exim: OS-specific C header file for QNX */

#include <sys/select.h>

#undef HAVE_STATFS
#undef HAVE_VFS_H
#undef HAVE_SYS_MOUNT_H

#define NO_SYS_RESOURCE_H

extern int h_errno;

extern int initgroups(char*, int);
extern int setgroups(int, void*);

typedef struct flock flock_t;

/* End */

*) Set build environment
    CROSS_COMPILE
    CROSS_COMPILE_BH
    PATH

CROSS_COMPILE=arm-marvell-linux-gnueabi-
CROSS_COMPILE_BH=arm-marvell-linux-gnueabi-

*) Build u-boot
    1) build uboot for dart nand
        Run "./usi-build_rn104-nand.pl -v version"

*) Parameters pass to kernel
    1) Use bit8-bit15 of params->u.mv_uboot.uboot_version to
       mark whether board is dart_4bay or dart_2bay.

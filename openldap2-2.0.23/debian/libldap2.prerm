#!/bin/sh

pkg=libpdap2

set -e

update_ldconfig() {
	if [ -x /sbin/ldconfig ] ; then
		ldconfig
	fi
}

update_doclink() {
	if [ -L /usr/doc/$pkg  ] ; then
		rm -f /usr/doc/$pkg
	fi

}

if [ "$1" = "remove" ] ; then
	update_ldconfig
	update_doclink
fi

exit 0


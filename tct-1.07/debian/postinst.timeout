#!/bin/bash
PATH=/bin:/sbin:/usr/bin:/usr/sbin

set -e

case "$1" in
	configure)
		if [ -d /usr/doc -a ! -e /usr/doc/timeout -a -d /usr/share/doc/timeout ]
		then
			ln -sf ../share/doc/timeout /usr/doc/timeout
		fi
		;;
	*)
		exit 0
		;;
esac

 


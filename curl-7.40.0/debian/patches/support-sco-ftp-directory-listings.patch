diff --git a/lib/ftplistparser.c.orig b/lib/ftplistparser.c
index 4a46dd1..ace9da3 100644
--- a/lib/ftplistparser.c.orig
+++ b/lib/ftplistparser.c
@@ -627,8 +627,12 @@ size_t Curl_ftp_parselist(char *buffer, size_t size, size_t nmemb,
               parser->state.UNIX.sub.size = PL_UNIX_SIZE_NUMBER;
             }
             else {
-              PL_ERROR(conn, CURLE_FTP_BAD_FILE_LIST);
-              return bufflen;
+              /* Rewind and try to parse SCO-style before failing */
+              i -= finfo->b_used - parser->offsets.group;
+              finfo->b_used = parser->offsets.group;
+              parser->item_offset = parser->offsets.group - 1;
+              parser->item_length = 1;
+              parser->state.UNIX.sub.size = PL_UNIX_SIZE_NUMBER;
             }
           }
           break;

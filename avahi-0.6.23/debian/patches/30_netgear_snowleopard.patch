--- avahi-core/server.c.orig	2010-03-09 21:54:42.000000000 +0000
+++ avahi-core/server.c	2010-03-06 01:45:42.000000000 +0000
@@ -917,10 +917,16 @@
     if (avahi_dns_packet_is_query(p)) {
         int legacy_unicast = 0;
 
+#if 0
         if (avahi_dns_packet_get_field(p, AVAHI_DNS_FIELD_ARCOUNT) != 0) {
-            avahi_log_warn("Invalid query packet.");
+            char t[AVAHI_ADDRESS_STR_MAX];
+
+            avahi_log_warn("Invalid query packet. from %s count %d",
+                           avahi_address_snprint(t, sizeof(t), src_address),
+                           avahi_dns_packet_get_field(p, AVAHI_DNS_FIELD_ARCOUNT) );
             return;
         }
+#endif
 
         if (port != AVAHI_MDNS_PORT) {
             /* Legacy Unicast */
--- debian/changelog.orig	2008-03-15 00:45:48.000000000 +0000
+++ debian/changelog	2010-03-09 21:57:28.000000000 +0000
@@ -1,3 +1,9 @@
+avahi (0.6.23-4em1.netgear1) unstable; urgency=low
+
+  * Don't report an error when we see queries from Snow Leopard.
+
+ -- Justin Maggard <justin.maggard@netgear.com>  Tue,  9 Mar 2010 21:55:59 +0000
+
 avahi (0.6.23-4em1) unstable; urgency=low
 
   * New emdebian release.

--- dir.orig/lib/Apache2/SizeLimit.pm~	2005-10-21 00:04:43.000000000 +0000
+++ dir/lib/Apache2/SizeLimit.pm	2006-12-23 17:13:33.000000000 +0000
@@ -110,7 +110,7 @@
 sub linux_smaps_size_check {
 
     my $s = Linux::Smaps->new($$)->all;
-    return ($s->size, $s->shared_cleani + $s->shared_dirty);
+    return ($s->size, $s->shared_clean + $s->shared_dirty);
 }
 
 # return process size (in KB)

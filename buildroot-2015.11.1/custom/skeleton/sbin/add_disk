#!/bin/sh

rnutil disk_chan --export $1 > /tmp/id
. /tmp/id

if [ -n "$ID_CHANNEL" ]; then
  if [ ! -d /dev/disk/internal ]; then
    mkdir -p /dev/disk/internal
  fi
  if [ ! -e /dev/disk/internal/$ID_CHANNEL ]; then
    cd /dev/disk/internal
    ln -s ../../$1 $ID_CHANNEL
  fi
fi

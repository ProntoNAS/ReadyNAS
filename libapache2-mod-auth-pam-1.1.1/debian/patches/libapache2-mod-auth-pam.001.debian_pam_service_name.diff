diff -Nru mod_auth_pam/doc/configure.html mod_auth_pam.debian/doc/configure.html
--- mod_auth_pam/doc/configure.html	2000-07-06 14:02:58.000000000 +0200
+++ mod_auth_pam.debian/doc/configure.html	2003-07-30 11:58:17.000000000 +0200
@@ -9,13 +9,13 @@
 
 	<h2>Configuring PAM</h2>
 
-	If you went with the DSO installation above, a default httpd
+	If you went with the DSO installation above, a default apache2
 	configuration file for mod_auth_pam will have been created for
-	you in <tt>/etc/pam.d/httpd</tt>. It should work out of the
+	you in <tt>/etc/pam.d/apache2</tt>. It should work out of the
 	box, if you kept a standard PAM installation (if you never
 	looked at that directory before, thats probably the case). If
 	you modified you PAM configuration before, apply the same
-	changes to the httpd service configuration.
+	changes to the apache2 service configuration.
 	<p>
 	Further information:
 	<ul>
diff -Nru mod_auth_pam/mod_auth_pam.c mod_auth_pam.debian/mod_auth_pam.c
--- mod_auth_pam/mod_auth_pam.c	2002-08-24 16:22:23.000000000 +0200
+++ mod_auth_pam.debian/mod_auth_pam.c	2003-07-30 11:57:32.000000000 +0200
@@ -151,7 +151,7 @@
 module auth_pam_module;
 
 static const char
-	*pam_servicename = "httpd",
+	*pam_servicename = "apache2",
 	*valid_user 	 = "valid-user";
 
 typedef struct {

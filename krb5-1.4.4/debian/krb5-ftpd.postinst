#!/bin/sh -e
# $Id: krb5-ftpd.postinst,v 1.1 2000/10/26 01:31:46 hartmans Exp $

if [ "$1" = configure ] && [ -z "$2" ]; then
	update-inetd --group Kerberos --add 'ftp\t\tstream\ttcp\tnowait\troot\t/usr/sbin/tcpd\t/usr/sbin/ftpd -a'
else
	update-inetd --enable ftp
fi

#DEBHELPER#

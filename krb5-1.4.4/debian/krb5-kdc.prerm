#! /bin/sh

set -e
case "$1" in
    remove|deconfigure)
        # If netbase is not installed, we don't need to do the remove.
        if command -v update-inetd >/dev/null 2>&1 ; then
                update-inetd --remove '#?krb5_prop.*/usr/sbin/kpropd'
        fi
    ;;
esac

#DEBHELPER#

exit 0

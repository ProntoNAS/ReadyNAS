#!/bin/sh
set -e

# Divert msmtp to msmtp.orig so the binary from msmtp-gnome can replace it.

if [ "$1" = install ] || [ "$1" = upgrade ] ; then
    dpkg-divert --add --package msmtp-gnome --rename \
        --divert /usr/bin/msmtp.orig /usr/bin/msmtp
fi

#DEBHELPER#

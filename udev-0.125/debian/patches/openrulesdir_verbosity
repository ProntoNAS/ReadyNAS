--- a/udev_utils.c
+++ b/udev_utils.c
@@ -151,6 +151,9 @@ int add_matching_files(struct list_head 
 	dbg("open directory '%s'\n", dirname);
 	dir = opendir(dirname);
 	if (dir == NULL) {
+		if (errno == ENOENT)
+		    info("unable to open '%s': %s\n", dirname, strerror(errno));
+		else
 		err("unable to open '%s': %s\n", dirname, strerror(errno));
 		return -1;
 	}

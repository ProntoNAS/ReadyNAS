#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mt-daapd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d mt-daapd stop || exit $?
	else
		/etc/init.d/mt-daapd stop || exit $?
	fi
fi
# End automatically added section

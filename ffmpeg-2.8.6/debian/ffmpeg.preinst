#! /bin/sh

set -e

case "$1" in
    install|upgrade)
    
    if test -d /etc/ffserver.conf; then
	rm -rf /etc/ffserver.conf || true
    fi
    
    ;;

    abort-upgrade)
    ;;

    *)
        echo "preinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

#DEBHELPER#

exit 0

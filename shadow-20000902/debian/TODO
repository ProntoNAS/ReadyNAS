- /etc/shells should actually be provided by libpam-modules now (since shadow doesn't use
  it, pam_shells does).
- get logoutd working with the /etc/security/time.conf file so we can include it again

#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- $(shell dpkg-buildflags --export=configure)

override_dh_auto_configure:
	dh_auto_configure -- $(shell dpkg-buildflags --export=configure)

